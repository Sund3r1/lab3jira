name: Bad CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-22.04
    
    steps:
    - uses: actions/checkout@v2  # 🦒 Устаревшая версия!
    
    - name: Install Python (как будто мы в саванне без воды)
      run: sudo apt-get update && sudo apt-get install -y python3.8 python3-pip
      
    - name: Install dependencies (каждый раз с нуля)
      run: pip3 install -r requirements.txt
      
    - name: Run tests with hardcoded credentials
      run: |
        export DB_PASSWORD="SuperSecret123!"  # 🦒 Видно всем с высоты!
        python3 -m pytest test_main.py
      
    - name: Build and push to production (всегда, даже ночью)
      run: |
        echo "Deploying to production..."
        # Пытаемся достать листья с самой высокой ветки без лестницы
        curl -X POST https://api.production.com/deploy \
          -H "Authorization: Bearer ${{ secrets.PROD_TOKEN }}" \
          -d '{"force": true}'
      
    - name: Cleanup (никогда не работает правильно)
      if: always()
      run: |
        rm -rf node_modules/  # 🦒 Зачем node_modules в Python проекте?
        rm -f *.log
